import './ToolBar.css';

const ToolBar = () => {
    return (
        <div className="toolBar">
            <h1 className="toolBar-title">Guest Book</h1>
        </div>
    );
};

export default ToolBar;